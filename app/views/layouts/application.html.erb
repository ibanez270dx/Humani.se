<!DOCTYPE html>
<html class="application <%= "#{controller.controller_path.split('/').join(' ')} #{controller.action_name.gsub(/_/, '-')}" %>">
  <head>
    <title><%= @title || "Humani.se" %></title>
    <link href='http://fonts.googleapis.com/css?family=Fjalla+One|Open+Sans:400,700,800|PT+Sans+Caption|Cabin|Gloria+Hallelujah|Homenaje|Special+Elite|Economica|Play:700|Noto+Sans|Quicksand' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <script type="text/javascript" src="//api.filepicker.io/v1/filepicker.js"></script>
    <%= csrf_meta_tags %>
    <%= yield :head %>
  </head>
  <body>
    <%= yield :root %>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to 'HUMANI.SE', about_path, class: 'brand' %>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <li class="<%= 'active' if request.url==about_url %>">
                <%= link_to 'About', about_path %>
              </li>
              <li class="<%= 'active' if request.url==blog_url %>">
                <%= link_to 'Blog', blog_path %>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="container content-section">
      <%= yield %>
    </div>

    <div class="container">
      <footer>
        <%= yield :footer %>
      </footer>
    </div>
  </body>
</html>
